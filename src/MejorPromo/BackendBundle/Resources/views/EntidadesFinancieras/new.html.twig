{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EntidadesFinancieras creation</h1>

    <form action="{{ path('entidadesfinancieras_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('entidadesfinancieras') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
